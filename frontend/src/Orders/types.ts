export type Product = {
    id: number;
    name: string;
    prince: number;
    description: string;
    imageUrl: string;
}

export type OrderLocationdata = {
    latitude: number;
    longitude: number;
    address: string;
}